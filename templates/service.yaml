apiVersion: v1
kind: Service
metadata:
  name: {{ include "defikit-helm.fullname" . }}
  labels:
    {{- include "defikit-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "defikit-helm.selectorLabels" . | nindent 4 }}
