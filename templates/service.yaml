apiVersion: v1
kind: Service
metadata:
  name: {{ include "prepumpfunbe.fullname" . }}
  labels:
    {{- include "prepumpfunbe.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "prepumpfunbe.selectorLabels" . | nindent 4 }}
