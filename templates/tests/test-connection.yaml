apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "prepumpfunbe.fullname" . }}-test-connection"
  labels:
    {{- include "prepumpfunbe.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "prepumpfunbe.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
