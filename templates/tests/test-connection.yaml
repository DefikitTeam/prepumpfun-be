apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "defikit-helm.fullname" . }}-test-connection"
  labels:
    {{- include "defikit-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "defikit-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
